/**
 * Filter interview problems based on ... (to be determined based on data format)
 */

import React from 'react'

function InterviewFilter(problems) {
  return (
    <div>
      <h1>Interview Filter</h1>
    </div>
  )
}

export default InterviewFilter
